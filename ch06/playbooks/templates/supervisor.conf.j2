[group:web]
programs=cms

[program:cms]
command={{ venv_path }}/bin/gunicorn -c gunicorn.conf.py -p gunicorn.pid cms.wsgi
directory={{ proj_path }}
user={{ user }}
autostart=true
autorestart=true
redirect_stderr=true
environment=LANG="{{ locale }}",LC_ALL="{{ locale }}",LC_LANG="{{ locale }}"
